package dev.millenialsoftwares.utils.papermc.connector;

import org.bukkit.plugin.InvalidPluginException;
import org.bukkit.plugin.java.JavaPlugin;

/**
 *
 */
public class Connector {

    public static Chiv init(JavaPlugin javaPlugin) throws InvalidPluginException {
        if (PaperAppLoader.)

        if (!javaPlugin.isEnabled())
        throw new InvalidPluginException("Could not find " + javaPlugin.getName() + " in plugins folder, please ");

        return null;
    }
}
